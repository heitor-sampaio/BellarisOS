# Dependencies
node_modules
.pnpm-store

# Build outputs
.next
dist
out
.expo
.expo-shared

# Turbo
.turbo

# Supabase
supabase/.temp
**/supabase/.temp

# Environment
.env
.env.local
.env.*.local

# Saída da sonda da uazapi: respostas cruas da API, com identificadores de
# instância. As credenciais já saem mascaradas do script, mas o arquivo não tem
# por que ser versionado.
uazapi-probe-*.json

# OS
.DS_Store
Thumbs.db

# Prints / screenshots locais (uso de debug)
prints/

# IDE
.vscode
.idea

# Claude Code
.claude/

# Firebase — não commitar; arquivo contém API key do Android client
apps/native/android/app/google-services.json
# Equivalente iOS (caso seja adicionado futuramente)
apps/native/ios/App/App/GoogleService-Info.plist

# Android/iOS signing keys — nunca commitar
*.keystore
*.jks
*.p12
*.p8
*.cer

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Testes automatizados
# A sessão do E2E é uma credencial viva (cookies do Supabase) — nunca no repo.
apps/web/e2e/.auth/
apps/web/test-results/
apps/web/playwright-report/

# Saída de sondas — payloads crus de provedor, com dados de clientes reais.
# Nunca versionar: a sonda existe para ser lida e apagada.
uazapi-*-bruto.json
