-- Seed de desenvolvimento — EstéticaOS
-- Dados mínimos para desenvolvimento local. Nunca usar em produção.

-- Os usuários Supabase Auth precisam ser criados via dashboard ou CLI antes de rodar este seed.
-- Substitua os UUIDs abaixo pelos IDs reais dos usuários criados no Supabase Auth.

-- Depois de inserir, chamar as functions de claims:
-- select public.set_user_claims('<auth_id_admin>', '<tenant_id>', null, 'NETWORK_ADMIN');
-- select public.set_user_claims('<auth_id_gerente>', '<tenant_id>', '<branch_id>', 'BRANCH_ADMIN');
-- select public.set_client_claims('<auth_id_cliente>', '<client_id>');
